So when a student clicks:

Download File

the backend serves the file from the uploads folder.

Alternatives (later)

In production you could store files in:

Cloudinary
AWS S3
Google Cloud Storage

Then MongoDB stores only the URL.

For your college project:

backend/uploads/  ✅ Recommended

is the simplest and most common approach.

Can you show me your multer.js config and tell me whether uploaded PDFs are actually appearing inside the backend/uploads folder? That's the next thing to verify.